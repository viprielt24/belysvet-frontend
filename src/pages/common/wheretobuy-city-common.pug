include ../../components/select-custom/select-custom

//+select-custom(registrationCity, 'Укажите город/область', 'default select-custom--simple')

.select-custom.select-custom--default
  select.form-control.js-select-city
    - conter = 0;
    each item in registrationCity
      //- неправильное условие на отсутствие выделенного пункта
      if !item.selected & !conter
        option(placeholder) Укажите город/область
        - conter++
      if item.value
        option(value= item.value selected= item.selected)= item.title