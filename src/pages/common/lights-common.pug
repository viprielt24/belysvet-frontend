include ../../components/lights-item/lights-item

.catalog.catalog--favourite
  .catalog__content
    .catalog__wrap
      each item, index in lights
        .catalog__col
          - item.link = null
          +lights-item(item)